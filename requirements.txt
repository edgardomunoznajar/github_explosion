# Core data analysis
numpy>=1.24
pandas>=2.0
scipy>=1.10

# Tests
pytest>=8.0

# Changepoint detection
ruptures>=1.1

# NLP / embeddings (optional — needed for Step 3 embeddings mode)
sentence-transformers>=2.2
scikit-learn>=1.3

# Visualization
matplotlib>=3.7

# BigQuery (optional — for running queries directly from Python)
# google-cloud-bigquery>=3.10
# google-cloud-bigquery-storage>=2.22
# db-dtypes>=1.1

# GPU acceleration (optional)
# torch>=2.0  # install via pytorch.org for CUDA support

# Clustering and dimensionality reduction (optional — for UMAP analysis)
# umap-learn>=0.5
# hdbscan>=0.8
